apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: "dashboard"
  namespace: kubernetes-dashboard
spec:
  rules:
  - host: dashboard.upenn.club
    http:
      paths:
      - path: /
        backend:
          serviceName: kubernetes-dashboard
          servicePort: 80

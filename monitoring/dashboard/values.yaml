image:
  repository: kubernetesui/dashboard
  tag: v2.0.0-rc5

enableInsecureLogin: true

service:
  externalPort: 80
  internalPort: 80

ingress:
  enabled: true
  paths: ["/"]
  hosts: ["dashboard.pennlabs.org"]

rbac:
  clusterAdminRole: true
replicaCount: 1

image:
  repository: pennlabs/pennbasics
  tag: latest
  pullPolicy: IfNotPresent

service:
  port: 80

ingress:
  enabled: true
  domain: basics.upenn.club

secrets:
  secretEnv: basics-secrets

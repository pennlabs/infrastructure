# Generated by cdkactions. Do not modify
# Generated as part of the 'pg-s3-backup' stack.
name: Publish pg-s3-backup
on:
  push:
    paths:
      - docker/pg-s3-backup/**
jobs:
  publish-pg-s3-backup:
    name: Publish pg-s3-backup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: docker/setup-qemu-action@v1
      - uses: docker/setup-buildx-action@v1
      - name: Cache Docker layers
        uses: actions/cache@v2
        with:
          path: /tmp/.buildx-cache
          key: buildx-publish-pg-s3-backup
      - uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Build/Publish
        uses: docker/build-push-action@v2
        with:
          context: docker/pg-s3-backup
          file: docker/pg-s3-backup/Dockerfile
          push: ${{ github.ref == 'refs/heads/master' }}
          cache-from: type=local,src=/tmp/.buildx-cache,type=registry,ref=pennlabs/pg-s3-backup:latest
          cache-to: type=local,dest=/tmp/.buildx-cache
          tags: pennlabs/pg-s3-backup:latest,pennlabs/pg-s3-backup:${{ github.sha }}
